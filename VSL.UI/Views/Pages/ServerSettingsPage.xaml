<UserControl x:Class="VSL.UI.Views.Pages.ServerSettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:VSL.UI.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="860"
             d:DesignWidth="1180">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="2,0,0,12">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Style="{DynamicResource VslPageTitleStyle}" Text="服务器配置" />
                <TextBlock Style="{DynamicResource VslPageSubTitleStyle}" Text="可视化编辑 serverconfig.json 常用字段" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="UI语言" VerticalAlignment="Center" Margin="0,0,8,0" />
                <ComboBox Width="120" SelectedValue="{Binding UiLanguage}" SelectedValuePath="Tag">
                    <ComboBoxItem Content="中文" Tag="zh-CN" />
                    <ComboBoxItem Content="English" Tag="en-US" />
                </ComboBox>
                <Button Content="保存可视化配置" Height="32" Width="140" Margin="10,0,0,0" Style="{StaticResource VslToolbarButtonStyle}" Command="{Binding SaveCommonConfigCommand}" />
            </StackPanel>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <ui:Card>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="服务器名称" />
                        <TextBox Grid.Row="0" Grid.Column="0" Margin="0,22,16,8" Height="34" Text="{Binding ServerName}" />

                        <TextBlock Grid.Row="0" Grid.Column="1" Text="IP (可空)" />
                        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,22,16,8" Height="34" Text="{Binding Ip}" />

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="端口" />
                        <TextBox Grid.Row="0" Grid.Column="2" Margin="0,22,0,8" Height="34" Text="{Binding Port}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="最大玩家数" />
                        <TextBox Grid.Row="1" Grid.Column="0" Margin="0,22,16,8" Height="34" Text="{Binding MaxClients}" />

                        <TextBlock Grid.Row="1" Grid.Column="1" Text="密码 (可空)" />
                        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,22,16,8" Height="34" Text="{Binding Password}" />

                        <TextBlock Grid.Row="1" Grid.Column="2" Text="白名单模式 (0/1/2)" />
                        <TextBox Grid.Row="1" Grid.Column="2" Margin="0,22,0,8" Height="34" Text="{Binding WhitelistMode}" />

                        <CheckBox Grid.Row="2" Grid.Column="0" Content="公开服务器 (AdvertiseServer)" IsChecked="{Binding AdvertiseServer}" Margin="0,8,0,8" />
                        <CheckBox Grid.Row="2" Grid.Column="1" Content="允许PVP" IsChecked="{Binding AllowPvP}" Margin="0,8,0,8" />
                        <CheckBox Grid.Row="2" Grid.Column="2" Content="允许火势蔓延" IsChecked="{Binding AllowFireSpread}" Margin="0,8,0,8" />
                        <CheckBox Grid.Row="3" Grid.Column="0" Content="允许落块" IsChecked="{Binding AllowFallingBlocks}" Margin="0,8,0,0" />
                    </Grid>
                </ui:Card>

                <ui:Card>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="世界种子" />
                        <TextBox Grid.Row="0" Grid.Column="0" Margin="0,22,16,8" Height="34" Text="{Binding Seed}" />

                        <TextBlock Grid.Row="0" Grid.Column="1" Text="世界名称" />
                        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,22,16,8" Height="34" Text="{Binding WorldName}" />

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="世界高度" />
                        <TextBox Grid.Row="0" Grid.Column="2" Margin="0,22,0,8" Height="34" Text="{Binding WorldHeight}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="玩法代码" />
                        <TextBox Grid.Row="1" Grid.Column="0" Margin="0,22,16,8" Height="34" Text="{Binding PlayStyle}" />

                        <TextBlock Grid.Row="1" Grid.Column="1" Text="世界类型" />
                        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,22,16,8" Height="34" Text="{Binding WorldType}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="当前存档路径" />
                        <TextBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,22,0,0" Height="34" Text="{Binding SaveFileLocation}" />
                    </Grid>
                </ui:Card>

                <ui:Card>
                    <StackPanel>
                        <TextBlock FontSize="15" FontWeight="SemiBold" Text="世界规则（可视化子集）" />
                        <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding WorldRules}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:WorldRuleItemViewModel}">
                                    <Border BorderThickness="0,0,0,1" BorderBrush="{DynamicResource VslPanelBorderBrush}" Padding="8" Background="Transparent">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="220" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" Orientation="Vertical">
                                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding Key}" Foreground="{DynamicResource VslMutedTextBrush}" FontSize="11" />
                                            </StackPanel>

                                            <Grid Grid.Column="1">
                                                <CheckBox Content="启用" IsChecked="{Binding BoolValue, Mode=TwoWay}" VerticalAlignment="Center">
                                                    <CheckBox.Style>
                                                        <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsBoolean}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </CheckBox.Style>
                                                </CheckBox>

                                                <ComboBox ItemsSource="{Binding Choices}"
                                                          SelectedItem="{Binding Value, Mode=TwoWay}"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Left"
                                                          Width="220">
                                                    <ComboBox.Style>
                                                        <Style TargetType="ComboBox" BasedOn="{StaticResource VslComboBoxStyle}">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsChoice}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ComboBox.Style>
                                                </ComboBox>

                                                <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                         VerticalAlignment="Center"
                                                         HorizontalAlignment="Left"
                                                         Width="220"
                                                         Height="32">
                                                    <TextBox.Style>
                                                        <Style TargetType="TextBox" BasedOn="{StaticResource VslTextBoxStyle}">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsTextLike}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBox.Style>
                                                </TextBox>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ui:Card>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

