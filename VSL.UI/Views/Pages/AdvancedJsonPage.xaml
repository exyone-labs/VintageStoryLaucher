<UserControl x:Class="VSL.UI.Views.Pages.AdvancedJsonPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="860"
             d:DesignWidth="1180">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="2,0,0,12">
            <TextBlock Style="{DynamicResource VslPageTitleStyle}" Text="高级 JSON 编辑" />
            <TextBlock Style="{DynamicResource VslPageSubTitleStyle}"
                       Text="直接编辑完整配置，保存后会自动刷新当前档案。"/>
        </StackPanel>

        <ui:Card Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{DynamicResource VslActionBarHostStyle}">
                    <WrapPanel>
                        <Button Content="重新加载 JSON"
                                Style="{StaticResource VslToolbarButtonStyle}" Command="{Binding LoadRawJsonCommand}" />
                        <Button Content="保存 JSON"
                                Style="{StaticResource VslToolbarButtonStyle}" Command="{Binding SaveRawJsonCommand}" />
                    </WrapPanel>
                </Border>

                <TextBox Grid.Row="1"
                         Text="{Binding RawJsonText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="13"
                         Padding="10"
                         Background="#FFFFFF"
                         BorderBrush="{DynamicResource VslPanelBorderBrush}"
                         TextWrapping="NoWrap" />
            </Grid>
        </ui:Card>
    </Grid>
</UserControl>




